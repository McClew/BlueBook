# Scripted API Data Extraction

Unusual User-Agents (e.g., `Python-urllib`, `PowerShell`, or custom scripts) make high-frequency calls to the **Microsoft Graph API**. This suggests a script is "walking" the directory tree to pull data.
